{
  "name": "jason-schemer",
  "private": true,
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @schemer/server dev"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5",
    "ipaddr.js": "^2.2.0"
  }
}
